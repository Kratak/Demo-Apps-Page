import { CombineNameSpacesFields } from '../../interfaces/combineNameSpacesFields';
import { common } from './common';

const combineNameSpaces: CombineNameSpacesFields = {
  common,
};

export default combineNameSpaces;
